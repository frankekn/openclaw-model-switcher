{
  "id": "model-profile",
  "name": "Model Profile Switcher",
  "description": "Swap AGENTS.md / SOUL.md and related bootstrap files based on the active model.",
  "version": "1.0.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "profilesDir": {
        "type": "string",
        "description": "Path to the profiles directory (default: <plugin_dir>/profiles)"
      }
    }
  }
}
